{
  "name": "awp-scanner",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted Playwright scanner: reads PUBLIC Amazon pages with a real browser and answers the AWP Cloudflare Worker over an OUTBOUND WebSocket (no inbound port; Tailscale stays the only ingress).",
  "main": "src/client.js",
  "scripts": {
    "start": "node src/client.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "playwright": "1.49.0",
    "ws": "^8.18.0"
  }
}
